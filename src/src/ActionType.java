
//Osamah Alsumaitti
package src;

/**
 * ActionType is enum which consists of action types when admin logs in like reserve a room, cancel a booking, check in or checkout etc.
 */
public enum ActionType {
    Reserve,
    Cancel,
    Checkin,
    Checkout
}
